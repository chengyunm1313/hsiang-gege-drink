# 享哥哥手搖飲訂購系統 - 產品需求文件 (PRD)

## 📋 文件資訊

- **產品名稱**: 享哥哥手搖飲訂購系統
- **版本**: 1.0.0
- **最後更新**: 2025年11月7日
- **負責人**: hsuhsiang
- **狀態**: 生產環境

---

## 🎯 產品概述

### 產品定位
一個完整的線上手搖飲訂購系統，提供一頁式購物體驗，支援多商品訂購、自動資料記錄與郵件通知。

### 核心價值
- 🚀 **高效率**: 智能預設值（中杯/去冰/微糖），減少客戶選擇時間
- 🛒 **靈活性**: 支援購物車功能，可同時訂購多種飲料
- 🤖 **自動化**: 訂單自動記錄到 Google Sheets 並發送確認郵件
- 📱 **響應式**: 完美支援手機和電腦瀏覽

### 目標用戶
- 想要快速訂購手搖飲的顧客
- 需要訂購多杯飲料的團體訂單
- 偏好線上訂購的數位原生用戶

---

## 🏗️ 系統架構

### 技術棧
```
前端: HTML5 + CSS3 + Vanilla JavaScript
後端: Make.com (No-code automation)
資料庫: Google Sheets
郵件服務: Gmail API
AI服務: Google Gemini API (可選)
```

### 資料流程
```
客戶瀏覽器
    ↓ (填寫訂單)
前端 JavaScript 預處理
    ↓ (發送 JSON)
Make.com Webhook
    ↓ (分流處理)
    ├─→ Google Sheets (記錄訂單)
    └─→ Gmail (發送確認郵件)
         └─→ Gemini AI (可選，生成個性化郵件)
```

---

## 📦 功能規格

### 1. 客戶資料收集
**位置**: 頁面頂部表單

**必填欄位**:
| 欄位 | 類型 | 驗證規則 | 範例 |
|------|------|----------|------|
| 姓名 | 文字 | 非空 | 王小明 |
| 電話 | 電話號碼 | 非空 | 0912345678 |
| 郵件 | Email | 非空，Email格式 | example@gmail.com |

**欄位ID**:
- `customerName` (姓名)
- `phone` (電話)
- `email` (郵件)

### 2. 商品選擇系統

#### 2.1 飲料類別
```javascript
const categories = [
    "旺情鳳梨",      // 🍍
    "經典綠茶",      // 🍃
    "激推水果茶",    // 🍋
    "秋冬暖胃茶",    // 🧊
    "獨門強沙冰",    // 🥤
    "茶奶雙響炮",    // 🥛
    "品味咖啡紅茶"   // ☕
];
```

#### 2.2 商品選項
**必選欄位**:
| 選項 | 類型 | 預設值 | 選項 |
|------|------|--------|------|
| 類別 | 下拉選單 | 無 | 7種類別 |
| 品項 | 下拉選單 | 無 | 依類別動態載入 |
| 尺寸 | 下拉選單 | M (中杯) | M / L |
| 冰塊 | 下拉選單 | 去冰 | 正常冰/少冰/微冰/去冰/熱飲 |
| 甜度 | 下拉選單 | 微糖 | 正常糖/七分糖/半糖/三分糖/微糖/無糖 |
| 數量 | 數字輸入 | 1 | 1-10 |

**選填欄位**:
- 特殊需求 (textarea，例如：珍珠加量)

#### 2.3 價格計算
- **即時顯示**: 單價 × 數量 = 小計
- **自動更新**: 任何選項改變時重新計算
- **顯示位置**: 表單底部

### 3. 購物車功能

#### 3.1 購物車狀態顯示
```javascript
購物車數量徽章: 顯示總杯數
購物車總金額: 顯示即時總額
```

#### 3.2 加入購物車邏輯
```javascript
// 驗證條件
if (category && drink && size && ice && sugar) {
    // 建立商品物件
    const cartItem = {
        id: 時間戳記 + 亂數,
        category: 類別,
        drink: 品項名稱,
        size: 尺寸,
        ice: 冰塊,
        sugar: 甜度,
        quantity: 數量,
        notes: 備註,
        unitPrice: 單價,
        itemTotal: 小計
    };
    
    // 加入購物車陣列
    cart.push(cartItem);
    
    // 更新顯示
    updateCartDisplay();
    renderCartItems();
    
    // 清除表單
    clearCurrentItem();
    
    // 顯示成功訊息
    showNotification('✅ 已加入購物車！');
}
```

#### 3.3 購物車商品管理
**功能**:
- ➕ 增加數量 (最多10杯)
- ➖ 減少數量 (最少1杯)
- 🗑️ 移除商品
- 🧹 清空購物車

**商品卡片顯示**:
```
┌─────────────────────────────────┐
│ 品項名稱              NT$ 小計  │
│ 🏷️ 類別 📏 尺寸 ❄️ 冰塊 🍯 甜度│
│ 備註: xxx                       │
│ [－] 數量: X [＋]     [🗑️ 移除] │
└─────────────────────────────────┘
```

### 4. 資料預處理系統

#### 4.1 前端預處理變數
**為什麼在前端預處理？**
- ✅ 減少 Make.com 處理負擔
- ✅ 避免 Make.com 函數限制
- ✅ 確保資料格式一致性
- ✅ 降低自動化成本

**預處理變數列表**:
```javascript
{
    // === 基本資訊 ===
    orderId: "CO" + 時間戳記 + 亂數,
    orderTime: "2025/11/07 15:30:45",
    customerName: "王小明",
    email: "example@gmail.com",
    phone: "0912345678",
    
    // === 商品陣列 ===
    items: [...],  // 完整商品物件陣列
    
    // === 統計資訊 ===
    totalItems: 9,           // 總杯數
    totalAmount: 530,        // 總金額
    itemTypes: 3,            // 商品種類數
    avgPrice: 59,            // 平均單價
    
    // === 客戶分級 ===
    customerTier: "VIP",     // VIP/Gold/Regular
    // 規則: >=500:VIP, >=200:Gold, 其他:Regular
    
    // === 格式化字串 ===
    itemsDetail: "HPP鮮榨梨可樂達(M杯)x1杯=NT$70 | 皇室奶昔(M杯)x4杯=NT$320",
    itemsSummary: "HPP鮮榨梨可樂達(M)x1, 皇室奶昔(M)x4",
    categoryList: "旺情鳳梨, 獨門強沙冰, 經典綠茶",
    icePreference: "少冰, 少冰, 少冰",
    sugarPreference: "七分糖, 七分糖, 七分糖",
    orderSummary: "3種商品，共9杯，總計NT$530",
    firstDrink: "HPP鮮榨梨可樂達",
    firstCategory: "旺情鳳梨"
}
```

#### 4.2 預處理函數實作
```javascript
// 客戶分級
let customerTier = "Regular";
if (totalAmount >= 500) customerTier = "VIP";
else if (totalAmount >= 200) customerTier = "Gold";

// 商品明細格式化
const itemsDetail = cart.map(item => 
    `${item.drink}(${item.size}杯)x${item.quantity}杯=NT$${item.itemTotal}`
).join(' | ');

// 簡化摘要
const itemsSummary = cart.map(item => 
    `${item.drink}(${item.size})x${item.quantity}`
).join(', ');

// 分類列表（去重）
const categoryList = [...new Set(cart.map(item => item.category))].join(', ');

// 偏好統計
const icePreference = cart.map(item => item.ice).join(', ');
const sugarPreference = cart.map(item => item.sugar).join(', ');
```

### 5. 結帳流程

#### 5.1 驗證檢查
```javascript
function handleCheckout() {
    // 1. 檢查購物車
    if (cart.length === 0) {
        alert('購物車是空的，請先選購商品');
        return;
    }
    
    // 2. 驗證客戶資料
    if (!validateCustomerInfo()) {
        alert('請填寫完整的基本資料');
        return;
    }
    
    // 3. 顯示載入狀態
    showLoadingState();
    
    // 4. 發送資料到 webhook
    submitOrder();
}
```

#### 5.2 訂單提交
```javascript
async function submitOrder() {
    try {
        const response = await fetch(WEBHOOK_URL, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify(orderData)
        });
        
        if (response.ok) {
            showSuccessState();
            clearCart();
        } else {
            throw new Error('訂單提交失敗');
        }
    } catch (error) {
        showErrorState(error);
    }
}
```

---

## 🔌 Make.com 整合規格

### Webhook 設定
**URL 格式**: `https://hook.us2.make.com/xxxxx`

**接收資料格式**:
```json
{
  "orderId": "CO35703794",
  "orderTime": "2025/11/07 15:30:45",
  "customerName": "王小明",
  "email": "example@gmail.com",
  "phone": "0912345678",
  "customerTier": "VIP",
  "totalItems": 9,
  "totalAmount": 530,
  "avgPrice": 59,
  "itemTypes": 3,
  "items": [...],
  "itemsDetail": "...",
  "itemsSummary": "...",
  "categoryList": "...",
  "icePreference": "...",
  "sugarPreference": "...",
  "orderSummary": "...",
  "firstDrink": "...",
  "firstCategory": "..."
}
```

### Google Sheets 欄位對應

| Google Sheets 欄位 | Make.com 變數 | 資料類型 |
|-------------------|---------------|---------|
| 訂單編號 (A) | `{{orderId}}` | 文字 |
| 訂購時間 (B) | `{{orderTime}}` | 日期時間 |
| 客戶姓名 (C) | `{{customerName}}` | 文字 |
| 電子郵件 (D) | `{{email}}` | Email |
| 電話號碼 (E) | `{{phone}}` | 文字 |
| 客戶等級 (F) | `{{customerTier}}` | 文字 |
| 商品種類數 (G) | `{{itemTypes}}` | 數字 |
| 商品總杯數 (H) | `{{totalItems}}` | 數字 |
| 訂單總金額 (I) | `{{totalAmount}}` | 數字 |
| 平均單價 (J) | `{{avgPrice}}` | 數字 |
| 商品分類 (K) | `{{categoryList}}` | 文字 |
| 商品明細 (L) | `{{itemsDetail}}` | 文字 |
| 冰塊偏好 (M) | `{{icePreference}}` | 文字 |
| 甜度偏好 (N) | `{{sugarPreference}}` | 文字 |
| 第一商品 (O) | `{{firstDrink}}` | 文字 |
| 訂單摘要 (P) | `{{orderSummary}}` | 文字 |

### Gmail 郵件模板

#### 簡易版本（推薦）
```html
<div style="font-family: Arial; max-width: 600px; margin: 0 auto;">
    <div style="background: #74b9ff; color: white; padding: 20px; text-align: center;">
        <h1>🥤 享哥哥手搖飲</h1>
        <h2>訂單確認</h2>
    </div>
    
    <div style="padding: 20px; background: #f8f9fa;">
        <p>親愛的 <strong>{{customerName}}</strong>，</p>
        
        <h3>📋 訂單資訊</h3>
        <p>訂單編號：{{orderId}}</p>
        <p>訂購時間：{{orderTime}}</p>
        
        <h3>🧊 訂購商品</h3>
        <div style="background: white; padding: 15px;">
            <p>{{itemsDetail}}</p>
        </div>
        
        <div style="background: #00b894; color: white; padding: 15px; text-align: center;">
            <h3>{{orderSummary}}</h3>
            <p>平均每杯 NT$ {{avgPrice}} | {{customerTier}} 會員</p>
        </div>
        
        <p>⏰ 預計準備時間：15-20 分鐘</p>
    </div>
</div>
```

#### AI 版本（可選）
使用 Gemini API 生成個性化郵件

**API 端點**: `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent`

**提示詞要點**:
- 明確指令：只輸出 HTML
- 禁止說明文字
- 直接以 `<!DOCTYPE html>` 開頭
- 包含完整 inline CSS
- 響應式設計
- 個性化問候（根據 customerTier）
- 商品推薦（根據 categoryList）

---

## 🎨 UI/UX 規格

### 設計系統

#### 顏色配置
```css
主色調（藍色系）:
  - Primary: #74b9ff
  - Dark: #0984e3
  - Gradient: linear-gradient(135deg, #74b9ff 0%, #0984e3 100%)

強調色（橙色系）:
  - Primary: #fdcb6e
  - Dark: #e17055
  - Gradient: linear-gradient(135deg, #fdcb6e 0%, #e17055 100%)

功能色:
  - Success: #00b894
  - Danger: #e17055
  - Warning: #fdcb6e
  - Info: #74b9ff
  - Gray: #636e72
```

#### 間距系統
```css
padding/margin:
  - XS: 5px
  - S: 10px
  - M: 15px
  - L: 20px
  - XL: 30px
```

#### 圓角系統
```css
border-radius:
  - Small: 5px
  - Medium: 8px
  - Large: 10px
  - XLarge: 15px
  - Round: 20px
  - Circle: 50%
```

### 響應式斷點
```css
@media (max-width: 768px) {
  /* 平板與手機 */
  - 單欄布局
  - 增大按鈕尺寸
  - 調整字體大小
}
```

### 互動效果
```css
按鈕 hover:
  - transform: translateY(-2px)
  - box-shadow: 0 5px 15px rgba(...)

輸入框 focus:
  - border-color: #74b9ff
  - box-shadow: 0 0 0 3px rgba(116, 185, 255, 0.1)

購物車商品 hover:
  - border-color: #74b9ff
```

---

## 📱 使用者流程

### 主要流程
```
1. 進入網站
   ↓
2. 填寫基本資料（姓名、電話、郵件）
   ↓
3. 選擇飲料類別
   ↓
4. 選擇飲料品項
   ↓
5. 確認/調整 尺寸、冰塊、甜度（有預設值）
   ↓
6. 設定數量、備註（可選）
   ↓
7. 加入購物車
   ↓
8. 重複步驟 3-7（可選）
   ↓
9. 查看購物車
   ↓
10. 調整商品數量/移除（可選）
   ↓
11. 點擊結帳
   ↓
12. 顯示載入動畫
   ↓
13. 顯示成功訊息
   ↓
14. 收到確認郵件
```

### 錯誤處理流程
```
購物車空 → 提示「請先選購商品」
資料未填 → 提示「請填寫完整資料」
網路錯誤 → 提示「訂單提交失敗，請稍後再試」
```

---

## 🔧 維護指南

### 1. 更新飲料菜單
**位置**: `script.js` 第 1-40 行

```javascript
const drinkMenu = {
    "類別名稱": {
        "飲料名稱": { M: 中杯價格, L: 大杯價格 },
        // 新增更多飲料...
    },
    // 新增更多類別...
};
```

**步驟**:
1. 找到對應類別
2. 新增或修改飲料品項
3. 設定中杯 (M) 和大杯 (L) 價格
4. 儲存檔案並重新整理頁面測試

### 2. 更新 Webhook URL
**位置**: `script.js` 第 43 行

```javascript
const WEBHOOK_URL = 'https://hook.us2.make.com/你的webhook';
```

### 3. 修改預設值
**位置**: `script.js` DOMContentLoaded 事件

```javascript
document.getElementById('size').value = 'M';      // 預設中杯
document.getElementById('ice').value = '去冰';    // 預設去冰
document.getElementById('sugar').value = '微糖';   // 預設微糖
```

### 4. 調整客戶分級規則
**位置**: `script.js` handleCheckout 函數

```javascript
let customerTier = "Regular";
if (totalAmount >= 500) customerTier = "VIP";      // 修改這裡
else if (totalAmount >= 200) customerTier = "Gold"; // 修改這裡
```

### 5. 修改品牌資訊
**位置**: `index.html`

```html
<h1>🥤 享哥哥手搖飲 - 旺情鳳梨</h1>
<p>HPP 鮮榨梨可樂達 | 夏日限定特調</p>
```

### 6. 調整樣式
**位置**: `style.css`

**常見調整**:
- 顏色: 搜尋 `#74b9ff` 和 `#fdcb6e`
- 間距: 修改 `padding` 和 `margin`
- 字體: 修改 `font-family` 和 `font-size`

---

## 🧪 測試清單

### 功能測試
- [ ] 基本資料填寫與驗證
- [ ] 飲料類別切換
- [ ] 價格即時計算
- [ ] 加入購物車
- [ ] 購物車數量調整
- [ ] 移除商品
- [ ] 清空購物車
- [ ] 結帳流程
- [ ] 載入動畫顯示
- [ ] 成功訊息顯示

### 資料測試
- [ ] Google Sheets 記錄正確
- [ ] 郵件發送成功
- [ ] 多商品訂單處理
- [ ] 訂單編號唯一性
- [ ] 時間格式正確

### UI/UX 測試
- [ ] 手機版面正常
- [ ] 平板版面正常
- [ ] 電腦版面正常
- [ ] 按鈕 hover 效果
- [ ] 輸入框 focus 效果
- [ ] 錯誤訊息顯示

### 瀏覽器測試
- [ ] Chrome
- [ ] Safari
- [ ] Firefox
- [ ] Edge
- [ ] 手機瀏覽器

---

## 🐛 已知問題與限制

### 技術限制
1. **Make.com 免費版**: 每月 1000 次操作
2. **Gmail 限制**: 每日 500 封郵件
3. **前端驗證**: 僅基本驗證，建議加強後端驗證

### 功能限制
1. **單一配送地址**: 目前不支援多地址
2. **付款功能**: 尚未整合線上付款
3. **庫存管理**: 無庫存追蹤功能
4. **訂單追蹤**: 無即時訂單狀態更新

### 建議改進
1. 加入訂單狀態追蹤
2. 整合線上付款（如 LINE Pay、街口支付）
3. 加入會員系統
4. 實作訂單歷史記錄
5. 加入推薦系統

---

## 📊 效能指標

### 載入效能
- 首次載入: < 2 秒
- 互動回應: < 100ms
- Webhook 回應: < 3 秒

### 成本估算
```
Make.com: 免費版 (1000次/月)
Google Sheets: 免費
Gmail: 免費 (500封/日)
Gemini API: $0.001-0.002/次 (可選)

預估月成本: $0 - $5 USD
```

---

## 🔐 安全性考量

### 現有措施
1. HTTPS 連線
2. 前端基本驗證
3. Make.com webhook 驗證

### 建議加強
1. 加入 CAPTCHA 防止機器人
2. Rate limiting (請求頻率限制)
3. 資料加密傳輸
4. 輸入消毒 (Sanitization)

---

## 📞 支援與聯絡

### 技術支援
- **檔案位置**: `/Users/hsuhsiang/Desktop/project/AI coding/project/hsiang gege drink/`
- **Make.com Scenario**: 享哥哥手搖飲店 vibecoding

### 常用資源
- [Make.com 文件](https://www.make.com/en/help)
- [Google Sheets API](https://developers.google.com/sheets/api)
- [Gmail API](https://developers.google.com/gmail/api)
- [Gemini API](https://ai.google.dev/)

---

## 📝 版本歷史

### v1.0.0 (2025-11-07)
- ✅ 一頁式訂購介面
- ✅ 購物車功能
- ✅ 前端資料預處理
- ✅ Make.com 自動化流程
- ✅ Google Sheets 整合
- ✅ Gmail 自動發信
- ✅ 響應式設計
- ✅ 智能預設值

---

## 🎯 未來規劃

### 短期 (1-3 個月)
- [ ] 加入會員登入功能
- [ ] 訂單歷史記錄
- [ ] 優惠券系統
- [ ] LINE 通知整合

### 中期 (3-6 個月)
- [ ] 線上付款整合
- [ ] 庫存管理系統
- [ ] 後台管理介面
- [ ] 數據分析儀表板

### 長期 (6-12 個月)
- [ ] 行動 App 開發
- [ ] AI 推薦系統
- [ ] 多店家支援
- [ ] 配送追蹤系統

---

**文件結束** | 最後更新: 2025年11月7日
